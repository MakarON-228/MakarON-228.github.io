# Сайт-резюме Makar Glazachev — техническое задание

Документ для работы в Claude Code. Лежит в корне репозитория, Claude Code ссылается на него по разделам:
«Прочитай SPEC.md §7.2 и реализуй». Короткие постоянные правила — в `CLAUDE.md`.

---

## 1. Цель и критерии успеха

**Задача сайта.** Рекрутер или тимлид за 30 секунд понимает, кто ты и что умеешь; инженер, у которого есть
5 минут, трогает твои проекты руками и убеждается, что это не слова.

Сайт готов, когда:

- на первом экране без скролла видны имя, роль, контакты, кнопка скачать CV и живая карта проектов;
- каждый проект из CV имеет интерактивную демонстрацию, показывающую именно ту технику, которую ты применял;
- Lighthouse (mobile): Performance ≥ 95, Accessibility = 100, Best Practices = 100, SEO = 100;
- всё работает с клавиатуры, на телефоне шириной 360px и при `prefers-reduced-motion`;
- в тексте нет ни одного утверждения, которое расходится с §6 (источник правды).

---

## 2. Стек и архитектура

### Выбор: Astro + TypeScript + острова

**Почему Astro.** Сайт в основном статический текст, а интерактив — это отдельные виджеты. Astro отдаёт
HTML без JavaScript и подгружает JS только для конкретного виджета и только когда он появляется на экране
(`client:visible`). Это ровно то, что нужно для «много интерактива + оптимизированно»: восемь демок на странице
не тормозят первый экран, потому что до скролла их кода в браузере просто нет.

| Слой | Решение | Почему не альтернатива |
|---|---|---|
| Фреймворк | **Astro** (актуальная версия) | Next.js тащит React-рантайм на каждую страницу; здесь он не нужен |
| Язык | **TypeScript** strict | ловит ошибки в логике демок до браузера |
| Интерактив | **Svelte 5** для UI-островов, **чистый TS + Canvas 2D** для анимаций и игры | рантайм Svelte ~3 КБ; игра и карта на канвасе не нуждаются во фреймворке |
| Стили | обычный CSS с токенами (custom properties), scoped-стили Astro | Tailwind здесь не окупается: айдентика кастомная |
| Аудио | Web Audio API | без библиотек |
| Шрифты | self-host через `@fontsource/*`, woff2, subset latin | Google Fonts добавляет лишний DNS-запрос и блокирует рендер |
| Хостинг | **GitHub Pages** через GitHub Actions (`withastro/action`) | бесплатно, адрес `makaron-228.github.io` |

### Структура репозитория

```
/
├─ SPEC.md, CLAUDE.md
├─ reference/                       ← только для сверки, на сайт не выкладывать:
│  ├─ alumina-scheme.png            ←   исходная схема реакций SIBUR
│  └─ tmh-map-original.png          ←   как выглядела карта в ТМХ
├─ public/
│  ├─ Makar_Glazachev_CV.pdf        ← текущий PDF из CV
│  ├─ og.png                        ← превью для соцсетей 1200×630
│  └─ favicon.svg
├─ src/
│  ├─ data/resume.ts                ← ВСЕ факты из §6, одним объектом; компоненты берут текст только отсюда
│  ├─ data/demo/                    ← данные демок; игрушечные помечены ILLUSTRATIVE
│  │  ├─ alumina-reactions.ts       ← готов: схема реакций SIBUR (§7.8)
│  │  └─ yandex-agents.ts           ← готов: 16 агентов и статусы платформы из кода (§7.5)
│  ├─ styles/tokens.css             ← палитра, типографика, отступы (§4)
│  ├─ layouts/Base.astro
│  ├─ components/                   ← статичные секции (.astro)
│  ├─ islands/                      ← интерактив (.svelte / .ts), грузится client:visible
│  │  ├─ TransitMap/                ← hero-карта (§7.1)
│  │  ├─ DepartureBoard/            ← табло цифр (§7.2)
│  │  ├─ LocoLookup/                ← поиск локомотива (§7.3)
│  │  ├─ RailMap/                   ← все станции России по дорогам, данные OSM (§7.4)
│  │  ├─ AgentPipeline/             ← агенты Яндекса (§7.5)
│  │  ├─ TwoModels/                 ← ансамбль CatBoost (§7.6)
│  │  ├─ ApiExplorer/               ← API Academic Profile (§7.7)
│  │  ├─ RouteFinder/               ← маршруты синтеза (§7.8)
│  │  ├─ PitchStaff/                ← YIN + нотный стан + игра на слух (§7.9)
│  │  ├─ RailSnake/                 ← мини-игра (§7.10)
│  │  └─ Terminal/                  ← терминал-пасхалка (§7.11)
│  └─ pages/index.astro, 404.astro
├─ scripts/build-stations.ts        ← выгрузка станций из OSM (§7.4)
└─ .github/workflows/deploy.yml
```

### Общие правила для каждого острова

- Грузится `client:visible` (терминал — `client:idle`).
- До гидратации на месте острова стоит **статичная версия** того же размера: картинка-заглушка или текст с
  результатом по умолчанию. Никакого сдвига вёрстки при загрузке.
- Анимации ставятся на паузу, когда остров вне экрана (IntersectionObserver) и когда вкладка скрыта
  (`visibilitychange`).
- `prefers-reduced-motion: reduce` → никаких автозапусков, только по действию пользователя; переходы мгновенные.
- Canvas: `devicePixelRatio` ограничен 2, перерисовка через `requestAnimationFrame`, без `setInterval`.
- Результаты демок объявляются скринридеру через `aria-live="polite"`.
- `localStorage` только внутри `try/catch`; без него всё работает.

---

## 3. Бюджет производительности и доступности

| Метрика | Бюджет |
|---|---|
| LCP (mobile, Slow 4G) | ≤ 1.8 s |
| CLS | < 0.05 |
| INP | < 150 ms |
| JS до первого скролла | ≤ 30 КБ gzip |
| Один остров | ≤ 40 КБ gzip |
| Вес первой загрузки без PDF | ≤ 350 КБ |
| Шрифты | ≤ 4 файла woff2, `font-display: swap`, preload только для display-начертания |

Доступность: контраст AA в обеих темах; видимый фокус; каждая демка управляется с клавиатуры; у канвасов
есть текстовая альтернатива (что показано и какой результат); `lang="en"`; skip-link «Skip to content».

---

## 4. Айдентика

### Концепция: схема метро

Твоя работа — это системы, по которым что-то течёт: вагоны по сети РЖД, данные по пайплайну, заявка по агентам,
звук через детектор. Сайт оформлен как **схема транспортных линий**: каждый проект — станция, темы — цветные
линии, а **Sirius University — пересадочный узел**, через который проходят все линии (все хакатоны были в Сириусе,
учёба там же). Это не декор: у тебя реально есть железнодорожная работа, и узел Сириуса — правда.

Карта кодирует настоящие данные: **по горизонтали — время** (2024 → 2026), станции стоят в месяц своего события.
Порядок по времени: SIBUR (Jul 2025) → Young Scientists (Nov 2025) → TMH Hackathon (Feb 2026) → Score Editor
(Mar 2026) → Yandex (Apr 2026) → TMH Internship (Jul 2026).

| Линия | Цвет | Станции |
|---|---|---|
| Rail | сигнальный красный | TMH Hackathon (Feb 2026) → TMH Internship (Jul 2026) |
| Science | тёмно-синий (как в CV) | Young Scientists Hackathon (Nov 2025) |
| Agents | зелёный | Yandex Vibe Coding (Apr 2026) |
| Craft | янтарный | SIBUR Feedstock System (Jul 2025) → Score Editor (Mar 2026) |
| — | узел | Sirius University (2024–2029) |

### Палитра

Нейтрали чуть смещены к синему — к акценту из PDF-резюме, чтобы сайт и CV выглядели одним брендом.

| Токен | Светлая | Тёмная | Роль |
|---|---|---|---|
| `--paper` | `#EDF0F2` | `#0B131A` | фон страницы |
| `--surface` | `#F8FAFB` | `#111D26` | поверхность демок |
| `--ink` | `#0E1B26` | `#E4ECF1` | основной текст |
| `--muted` | `#56697A` | `#8EA2B1` | вторичный текст |
| `--rule` | `#C7D1D9` | `#243644` | линейки, рамки |
| `--line-science` | `#134F78` | `#5BA3D9` | бренд-акцент, ссылки, цифры |
| `--line-rail` | `#C8372D` | `#F0604F` | линия Rail |
| `--line-agents` | `#1F8A5B` | `#3CC486` | линия Agents |
| `--line-craft` | `#C98A0E` | `#F2B53A` | линия Craft — **только заливки и линии, не текст** (мало контраста на светлом) |

Темы: светлая по умолчанию в `:root`, тёмная через `@media (prefers-color-scheme: dark)` и через
`[data-theme="dark"]` для переключателя. Выбор темы запоминается в `localStorage`.

### Типографика

Все три гарнитуры из дорожно-транспортной навигации — сайт буквально набран шрифтами указателей.

| Роль | Гарнитура | Где |
|---|---|---|
| Display | **Barlow Condensed** 700/800 — наследник калифорнийских дорожных знаков | имя, заголовки секций, названия станций, большие цифры |
| Body | **Overpass** 400/600 — наследник Highway Gothic | весь текст |
| Data | **Overpass Mono** 400/600 | табло, терминал, API-панель, подписи осей |

Проверь при установке, что пакеты `@fontsource/barlow-condensed`, `@fontsource/overpass`,
`@fontsource/overpass-mono` доступны; если какого-то нет — ближайшая замена того же характера.

Шкала: 14 / 16 / 18 / 22 / 28 / 40 / 64 / clamp(56px, 9vw, 120px) для имени. Строка текста ≤ 68 знаков.
Заголовкам `text-wrap: balance`. Цифры — `font-variant-numeric: tabular-nums`.

### Переносимые из CV приёмы

- Цифры-результаты выделены акцентным цветом (`--line-science`), жирным.
- Короткий акцентный брусок под именем.
- Капсула `3 entered · 3 won` — **одна на весь сайт**, в секции хакатонов (не дублировать в hero).

### Движение

Одна срежиссированная сцена: при загрузке по линиям hero-карты трогаются поезда. Всё остальное — микро-реакции
на наведение и клик. Никакого «появления секций при скролле» из `opacity: 0`: контент виден сразу.

### Чего избегать

Кислотный зелёный на чёрном, фиолетово-синие градиенты, эмодзи как маркеры, всё по центру, одинаковые скруглённые
карточки с тенью везде, нумерация 01/02/03 там, где нет последовательности.

---

## 5. Структура страницы

Одна длинная страница + якоря. Порядок:

1. **Hero** — имя, роль, одна фраза, контакты, «Download CV», карта линий (§7.1).
2. **Departure board** — табло с реальными цифрами (§7.2).
3. **Experience** — TMH, стажировка + демки §7.3 и §7.4.
4. **Hackathons** — три победы, у каждой своя демка: §7.5, §7.6, §7.7.
5. **Projects** — SIBUR (§7.8) и Score Editor (§7.9).
6. **Education** — Sirius как узел.
7. **Skills**.
8. **Depot** — мини-игра Rail Snake (§7.10).
9. **Contact** + футер.

**Навигация.** Липкая верхняя полоса оформлена как линия метро: станции = секции, маленький поезд едет по линии
по мере прокрутки страницы. На телефоне — компактная полоса без подписей, по тапу раскрывается список.
Кнопка темы и кнопка терминала `›_` справа.

**На мобильном** hero-карта перестраивается вертикально: время идёт сверху вниз.

---

## 6. Контент — источник правды

Весь текст сайта берётся отсюда. Это то же, что в PDF-резюме, с теми же ограничениями по авторству.
**Любое изменение фактов — сначала здесь.**

### 6.1 Правила авторства (обязательно)

| Проект | Можно утверждать | Нельзя утверждать |
|---|---|---|
| TMH Hackathon | ML engineer в команде Ascent; сам сделал прогнозную модель: CatBoostRegressor, 407,669 записей износа, две версии (базовые гипотезы / полный набор признаков команды), смешивание с подобранными весами по анализу ошибки по децилям, MSE 0.12, выдача через REST API | проверку гипотез, Kruskal–Wallis, Dunn, признаки кривизны/энтропии/климата — это работа аналитика команды |
| Young Scientists | единственный бэкенд-разработчик: FastAPI, 15 эндпоинтов, JWT, SQLAlchemy; **обслуживал** готовую модель рекомендаций команды | обучение модели TF-IDF + KNN |
| Yandex | капитан команды «AI Болид» из трёх человек и **единственный разработчик** — всю платформу написал сам, команда вместе готовила презентацию; платформа ведёт заявку от подачи до публичной витрины; 16 агентов (5 оценки, 9 deep research, 2 MCP-экспорта) — структура в `src/data/demo/yandex-agents.ts`, взята из кода | «все семь стадий жизненного цикла» — выделения ресурсов в Yandex Cloud в решении нет; не выпячивать MCP-интеграцию как сильную сторону |
| Score Editor | отображаемое имя **Score Editor**, ссылка — `github.com/MakarON-228/Note_redactor` | — |
| Везде | локация **Sirius**, не Sochi | — |

**Игрушечные данные.** Там, где демка работает не на реальных данных, в её углу стоит метка
**«Illustrative data»** и одна строка, что именно реальное (алгоритм) и что выдумано (цифры). Игрушечные цифры
никогда не подаются как результат.

**Данные ТМХ на сайт не попадают.** Карта станций строится из открытых данных OpenStreetMap (§7.4), демо поиска
локомотива — на выдуманном реестре. Ни выгрузок ТМХ, ни скриншотов их систем.

**Без дублей.** Пользователь не любит повторы: факт звучит один раз. Hero не пересказывает секции.

### 6.2 Шапка

- Name: **Makar Glazachev**
- Role: **Backend, Data & ML Engineer**
- Summary: *Works across the whole of a data product — the pipeline underneath, the service on top, the model in
  between — and ships it tested, containerised and documented.*
- Email: `makarglaza4ev@gmail.com` (показывать текстом + кнопка «Copy»)
- Telegram: `t.me/MaKaRollo`
- GitHub: `github.com/MakarON-228`
- Location: Sirius, Russia · English B2 · Russian native
- CV: `/Makar_Glazachev_CV.pdf`

### 6.3 Experience

**Transmashholding (TMH) — Data Engineering Intern** · Moscow · Jul 2026
Russia's largest rail rolling-stock manufacturer; joined after winning its hackathon.

- Built a **4,400-line ETL pipeline** turning raw Excel exports — locomotive movements, depot equipment, station
  coordinates — into **16 analysis-ready Parquet tables** on a versioned, documented schema (DuckDB, PyArrow, RapidFuzz).
- Modelled **30,146 movement events**, **20,866 locomotives** and 369 enterprises, and built a **209,638-row search
  index** resolving locomotive numbers with or without leading zeros and down to individual sections.
- Shipped an interactive map visualiser: stations plotted and filterable by railway, visit frequency per period,
  locomotives traced over time.
- Backed it with 12 pytest tests, ruff gates, Docker Compose packaging and written data-model and validation docs.

Repo: `github.com/MakarON-228/rzd_data_preparation_v2`

### 6.4 Hackathons — `3 entered · 3 won`

**Yandex Vibe Coding Hackathon — Winner — captain & sole developer** · Apr 2026
Platform taking a project proposal from intake to a public showcase, run by **16 agents** on YandexGPT: a review panel
of four personas scoring in parallel and a moderator issuing a weighted 0–100 verdict, a **nine-agent** deep-research
chain through architecture, roadmap, staffing and risk, and two MCP agents turning the result into Yandex Tracker and
SourceCraft work.

**TMH Hackathon, Sirius — Winner — ML engineer, team Ascent** · Feb 2026
Wheel-life track, one of three contested by **14 teams**. Owned the prediction model: CatBoostRegressor over
**407,669 wheel-wear records** in two variants — baseline hypotheses and the team's full feature set — blended with
fitted weights, as per-decile error showed neither held the full range. **MSE 0.12**, served over a REST API.
Repo: `github.com/Astal2307/TMHackathon_Ascent`

**Young Scientists Hackathon, Sirius — Winner, international field** · Nov 2025
*Academic Profile* — publication records harvested from Scopus, Web of Science, Google Scholar and Crossref, turned
into h-index and citation metrics, a knowledge graph and collaborator recommendations. **Sole backend engineer** —
FastAPI, 15 REST endpoints, JWT auth and SQLAlchemy models, serving the team's TF-IDF + KNN recommender over
**194,849 researchers**.
Repo: `github.com/brainstorm-sirius/academic-profile`

### 6.5 Projects

**Chemical Feedstock Selection System — built with SIBUR experts** · Python · Jul 2025
From warehouse stock and a reaction graph — substance transitions under given operations — the system enumerates
**every** route to a target compound by solving the corresponding linear systems, then ranks them by difficulty.

**Score Editor — sheet music editor** · C++17 · Qt6 · Mar 2026
Desktop score editor — notes, rests, accidentals, barlines and multi-staff layout under strict positioning rules,
PNG/PDF export, JSON projects. Live pitch input: a **hand-implemented YIN detector** places each note played into the
right staff position.
Repo: `github.com/MakarON-228/Note_redactor`

### 6.6 Education

**Sirius University of Science and Technology** · 2024 – 2029
**Design, Development and Management of Complex Information Systems** — five-year integrated programme

- Russia's first degree to combine secondary and higher education, admitting students straight after 9th grade,
  under an experimental legal regime in the Sirius Federal Territory.
- Core track: software architecture for critical infrastructure, systems analysis, enterprise systems, process automation.
- Industry projects from year one with Yandex, VK, Rosatom and Russian Railways; the final year is a full placement.

### 6.7 Skills

| Group | Items |
|---|---|
| Languages | Python, C++17, SQL |
| Backend | FastAPI, SQLAlchemy, Pydantic, REST API design, JWT authentication |
| Data | DuckDB, Parquet / PyArrow, pandas, NumPy, SciPy, RapidFuzz, ETL design |
| ML & AI | CatBoost, scikit-learn, model serving, multi-agent LLM systems, MCP |
| Tooling | Docker & Compose, Git, pytest, ruff, Qt6, CMake |

---

## 7. Интерактивные элементы — спецификации

Для каждого: что показывает о твоей работе → как взаимодействовать → данные → критерии приёмки.

### 7.1 Hero: карта линий

- **Показывает:** весь путь одним взглядом — линии, станции, время, узел Сириуса.
- **Вид:** SVG (чёткий на любом экране, доступный). Линии в стиле схемы метро: углы 45°/90°, скруглённые изгибы,
  станции — белые круги с обводкой цветом линии, пересадочный узел — капсула.
- **Движение:** по каждой линии бегут 1–2 «поезда» (точки со шлейфом) через `getPointAtLength`. При reduced motion
  поезда стоят на станциях.
- **Взаимодействие:** наведение/фокус на станцию → подсказка (название, дата, одна строка из §6); клик → плавный
  переход к секции. Станции — это `<a>` с `aria-label`, работают с Tab.
- **Приёмка:** станции на верных месяцах шкалы; на ширине < 720px карта вертикальная; без JS видна та же карта без
  анимации.

### 7.2 Departure board

- **Показывает:** реальные цифры из §6 в виде вокзального табло с перекидными ячейками.
- **Цифры:** 4,400-line ETL pipeline · 16 Parquet tables · 209,638-row search index · 407,669 wear records ·
  MSE 0.12 · 194,849 researchers served · 16 AI agents.
- **Взаимодействие:** при наведении/тапе ячейки строки перещёлкиваются и возвращаются к значению (split-flap).
  В покое значения видны всегда — анимация не прячет цифры.
- **Приёмка:** ни одной цифры, которой нет в §6; моноширинный шрифт, `tabular-nums`.

### 7.3 LocoLookup — поиск локомотива (TMH, стажировка)

- **Показывает:** реальную логику твоего поискового индекса из `rzd_data_preparation_v2`.
- **Логика (как в репозитории):** запрос нормализуется — пробелы убираются, регистр верхний, латинская `A` в конце
  номера превращается в кириллическую `А`. `61` и `0061` возвращают базовые локомотивы с заводским номером 0061;
  `0061А` возвращает конкретные секции `А`. Один заводской номер встречается у разных серий, поэтому в выдаче
  показаны серия, предприятие и тип результата (locomotive / section).
- **UI:** поле ввода с подсказками-примерами `61`, `0061`, `0061A`, `0061А`; под ним — «query key» после
  нормализации и таблица результатов.
- **Данные:** игрушечный реестр на 30–50 строк с реальными названиями серий (2ТЭ116, 3ЭС5К, ТЭМ18ДМ, ЧС7…) и
  выдуманными номерами и депо. Метка «Illustrative data — the normalisation logic is real».
- **Приёмка:** четыре примера выше дают ожидаемые выдачи; юнит-тесты на нормализацию (vitest).

### 7.4 RailMap — все станции России по дорогам (TMH, стажировка)

Показывает то, что умел твой визуализатор, но на **открытых данных**: все станции сети, фильтр и сортировка по
дорогам. Ничего из данных ТМХ и ничего с твоего компьютера — сайт собирает данные сам.

- **Ориентир:** `reference/tmh-map-original.png` — как выглядела карта в визуализаторе ТМХ (светлая подложка, ходы
  нарисованы цепочками точек-станций, цветные маркеры). Цель — лучше: ходы настоящими линиями, цвет по дороге,
  обе темы, фильтр и поиск. Картинку на сайт не выкладывать.
- **Источник:** OpenStreetMap, узлы `railway=station` на территории России (+ переключатель «показать платформы» —
  `railway=halt`). Лицензия ODbL → на карте обязательна подпись «© OpenStreetMap contributors».
- **Сборка данных:** скрипт `scripts/build-stations.ts` один раз тянет данные через Overpass API, нормализует
  (название, координаты, код ЕСР из `esr:user`, если есть), определяет дорогу и пишет
  `public/data/stations.json` + `public/data/railways.json` (сводка по дорогам). Результат **коммитится** в репозиторий
  с датой выгрузки — CI не ходит в Overpass при каждой сборке. Перезапуск вручную раз в несколько месяцев.
- **Определение дороги — главная сложность, проверять обязательно.** Первые цифры кода ЕСР исторически означали
  дорогу, но сейчас есть участки, где станции с одним префиксом относятся к разным дорогам — поэтому префикс только
  подсказка, не источник истины. Порядок:
  1. тег `operator` станции, если в нём названа одна из 16 дорог РЖД;
  2. иначе — связь с Wikidata (`wikidata=Q…`), свойство «оператор»/«часть»;
  3. иначе — ближайшая уже определённая станция по сети (или по расстоянию);
  4. ручные исправления — `data/railway-overrides.csv`.
  Скрипт печатает покрытие по каждому шагу. Неопределённые станции показываются в категории «Unassigned», а не
  раскидываются наугад.
- **16 дорог РЖД:** Октябрьская, Калининградская, Московская, Горьковская, Северная, Северо-Кавказская,
  Юго-Восточная, Приволжская, Куйбышевская, Свердловская, Южно-Уральская, Западно-Сибирская, Красноярская,
  Восточно-Сибирская, Забайкальская, Дальневосточная.
- **Тесты — станции-эталоны** (управления дорог): Санкт-Петербург-Главный → Октябрьская, Калининград-Пассажирский →
  Калининградская, Москва-Курская → Московская, Нижний Новгород-Московский → Горьковская, Ярославль-Главный →
  Северная, Ростов-Главный → Северо-Кавказская, Воронеж-1 → Юго-Восточная, Саратов-1 → Приволжская, Самара →
  Куйбышевская, Екатеринбург-Пассажирский → Свердловская, Челябинск-Главный → Южно-Уральская, Новосибирск-Главный →
  Западно-Сибирская, Красноярск-Пассажирский → Красноярская, Иркутск-Пассажирский → Восточно-Сибирская, Чита-2 →
  Забайкальская, Хабаровск-1 → Дальневосточная.
- **Отрисовка:** MapLibre GL JS (WebGL — десятки тысяч точек без тормозов). Подложка — **без сторонних тайлов**:
  собственный слой контура страны (Natural Earth, public domain) и, опционально, упрощённые главные ходы
  (`railway=rail` + `usage=main` из той же выгрузки). Цвета — токены сайта, обе темы.
- **Взаимодействие:**
  - панель дорог: список с числом станций, сортировка по количеству или по алфавиту; клик — фильтр и приближение к
    дороге; несколько дорог можно выбрать сразу;
  - цвет точки = дорога; при выборе одной дороги остальные гаснут;
  - поиск станции по названию с автодополнением → перелёт к ней;
  - наведение/тап — карточка: название, дорога, код ЕСР, тип (станция/платформа);
  - кластеризация на мелком масштабе.
- **Скорость:** MapLibre (~220 КБ gzip) и данные грузятся, только когда блок появляется на экране; до этого — статичная
  SVG-картинка тех же точек, сгенерированная при сборке. Данные ≤ 400 КБ gzip. Это исключение из бюджета острова
  (§3) — оправдано, потому что вне первого экрана.
- **Подпись под картой:** «Station data © OpenStreetMap contributors, extracted {дата}. Railway assignment derived
  from OSM tags and Wikidata; the original TMH visualiser worked on the company's internal data.»
- **Приёмка:** все 16 эталонов верны; доля «Unassigned» выведена и < 5 % (иначе — обсудить); фильтр мгновенный;
  работает на тач; первый экран сайта не грузит ни байта карты.

### 7.5 AgentPipeline — платформа агентов (Yandex)

Всё в этой демке — **настоящая структура из кода** команды, она уже лежит в `src/data/demo/yandex-agents.ts`:
16 агентов, кто кого читает и сколько символов, какие оценки вытаскиваются, статусы проекта, роли пользователей.
Выдуманы только текст заявки и ответы агентов — это симуляция.

- **Линия статусов проекта** (зелёная линия Agents): `draft → submitted → under_review → accepted_for_research →
  deep_research_running → deep_research_completed → on_showcase`; ответвления `revision_requested` (возврат на
  доработку) и `rejected` (тупик). Решение принимает человек-reviewer — агенты только советуют; это показать явно.
- **Станция «Review» — панель оценки:** четыре эксперта (Technical Analyst, Market Researcher, Innovator, Risk Assessor)
  загораются **одновременно** — в коде они идут параллельно в четыре потока; затем Moderator сводит их в балл 0–100 и
  вердикт APPROVE / REJECT / UNDECIDED.
- **Станция «Deep research» — цепочка из девяти:** Project Analyst → Research Strategist → Technical Researcher →
  Architect → Roadmap Manager → HR Specialist → Risk Analyst → Quality Reviewer → Synthesis Manager. Агенты
  срабатывают **по очереди**, счётчик «3 / 9» как в настоящем UI. Над цепочкой — дуги контекста: кто чьи выводы
  читает, толщина дуги = лимит символов из кода (например, Architect → Roadmap Manager 6000). Risk Analyst выдаёт
  GO / GO WITH CONDITIONS / NO-GO и feasibility score, Quality Reviewer — оценки качества и полноты.
- **Станция «Export»:** два MCP-агента расходятся на Yandex Tracker и SourceCraft.
- **Станция «Showcase»:** карточка проекта на публичной витрине, кнопка «Write to the project» открывает чат.
- **Управление:** выбор одной из 2–3 заготовленных заявок; «Run»; «Skip to result»; клик по любому агенту — его роль и
  что он читает на входе.
- **Метка:** «Simulation — scripted outputs. Agent roster, order, parallelism and context limits are taken from the
  team's code.» Модели в подписи: YandexGPT 5.1 Pro, DeepSeek V3.2 as an alternative profile.
- **Приёмка:** 16 агентов, порядок и параллельность как в коде; при reduced motion сразу финальное состояние.

### 7.6 TwoModels — почему две модели (TMH Hackathon)

- **Показывает:** твоё ключевое решение. Ошибка по децилям показала, что ни одна модель не держит весь диапазон,
  поэтому итоговый прогноз — **взвешенная сумма двух моделей с подобранными коэффициентами**:
  `ŷ = w_A · ŷ_baseline + w_B · ŷ_full`.
- **UI:** график MAE по децилям истинного значения: «Baseline hypotheses», «Full feature set», «Blended».
  Ползунок `w_A` (в демо `w_B = 1 − w_A` — упрощение, подписать); маркер «fitted» на значении, которое находит
  перебор по сетке на валидационной части синтетики. Кнопка «Fit weights» анимирует этот перебор.
  Рядом крупно реальная цифра: **MSE 0.12 on the real data**.
- **Данные:** синтетика с тяжёлым правым хвостом, где базовая модель точнее в середине, полная — на краях.
  Метка «Synthetic data — illustrates the method; the real result is MSE 0.12».
- **Приёмка:** ни одна синтетическая цифра не подписана как реальная; смешанная кривая на подобранном весе ниже
  обеих исходных в среднем; график по правилам — одна шкала, подписи не налезают, цвета из токенов.

### 7.7 ApiExplorer — бэкенд Academic Profile (Young Scientists)

- **Показывает:** именно твою часть — API. Это Swagger-подобная панель с реальными эндпоинтами из репозитория:

  | Method | Path |
  |---|---|
  | POST | `/auth/register` |
  | POST | `/auth/login` |
  | GET | `/users/me` |
  | PUT | `/users/interests` |
  | POST | `/users/{user_id}/publications/upload` |
  | GET | `/users/{user_id}/publications` |
  | DELETE | `/users/{user_id}/publications/{publication_id}` |
  | GET | `/search` |
  | GET | `/search/users` |
  | GET | `/search/authors` |
  | GET | `/authors/{author_id}/interests` |
  | GET | `/authors/{author_id}/profile` |
  | GET | `/health` |
  | POST | `/recommend` |
  | GET | `/knowledge-graph` |

- **UI:** слева список эндпоинтов, по клику — пример запроса и ответа (JSON с подсветкой). Для `/recommend` и
  `/knowledge-graph` справа рисуется маленький силовой граф учёных и интересов; клик по учёному → «вызов»
  `/recommend` → подсвечиваются 3 рекомендованных коллеги.
- **Данные:** 25–40 выдуманных учёных. Рекомендации в демке считаются в браузере косинусной близостью по
  интересам. Подпись: «Recommender model by the team; the API serving it is Makar's».
- **Приёмка:** все 15 эндпоинтов в списке; JSON-ответы по формату похожи на реальные схемы из
  `app/schemas.py` репозитория (Claude Code может их прочитать, если склонировать репо).

### 7.8 RouteFinder — маршруты синтеза (SIBUR)

- **Показывает:** перебор **всех** путей по графу превращений, расчёт требуемых масс, ранжирование по сложности и
  комбинирование источников, когда одного не хватает.
- **Граф — настоящий:** схема превращений модификаций оксида алюминия, с которой работала система. Уже перенесена в
  `src/data/demo/alumina-reactions.ts`: 21 вещество, 24 перехода с температурами, давлением, вакуумом и массовыми
  коэффициентами. Исходник для сверки — `reference/alumina-scheme.png` (на сайт не выкладывать, рисовать свой граф).
- **Контрольные значения для тестов** (посчитаны по файлу данных):
  - к α-Al₂O₃ ведут **29** простых маршрутов из всех вершин графа; из гиббсита — 5, из байерита — 2, из бёмита — 3;
  - по любому маршруту из Al(OH)₃ итоговый коэффициент **1.5301** кг на 1 кг Al₂O₃, из AlO(OH) — **1.1767**:
    масса не зависит от пути (сохраняется алюминий) — отличная проверка, что коэффициенты перемножаются верно;
  - на игрушечном складе максимум корунда: гиббсит 326.8 кг, байерит 78.4, бёмит 68.0, аморфный Al(OH)₃ 39.2,
    диаспор 34.0, нордстрандит 19.6 — всего **566 кг**. 300 кг закрываются одним гиббситом, 450 кг — только
    комбинацией, 600 кг — невыполнимо.
- **Сложность маршрута — иллюстративная модель** (исходная не восстановлена), веса регулируются ползунками прямо в
  демо: `шаг = 1 + w_T · T/1000 + w_P · [гидротермально, 70–90 MPa] + w_V · [вакуум] + 0.5 · [условия не указаны]`,
  маршрут = сумма шагов. Пользователь меняет, что значит «сложно», — и видит, как меняется рекомендация.
- **Комбинирование:** если лучшему маршруту не хватает склада — добирать массу следующими по сложности маршрутами
  из оставшихся запасов (жадно, по возрастанию сложности; каждый источник расходуется один раз на все свои маршруты).
  Это и есть «система линейных уравнений» из CV в упрощённом виде — подписать так.
- **UI:** слева склад (редактируемые остатки) и выбор цели — по умолчанию α-Al₂O₃, можно любую модификацию;
  по центру граф (слоями: гидроксиды → переходные оксиды → α), маршруты подсвечиваются при наведении; справа
  ранжированный список: цепочка с условиями над стрелками, требуемая масса сырья, сложность, хватает ли склада.
  Цепочки с Al(MOₙ)₃ показываются, но без массы («molar mass not defined in the scheme»).
- **Метка:** «Reaction scheme: the real alumina transformation graph the system worked on. Stock levels and the
  difficulty weights are illustrative.»
- **Приёмка:** все контрольные значения выше проходят в тестах vitest; при нехватке склада предложена комбинация или
  честное «not enough stock: max 566 kg».

### 7.9 PitchStaff — YIN на нотном стане (Score Editor) + игра на слух

- **Показывает:** твой собственный алгоритм из `src/audio/PitchDetector.cpp`, работающий в браузере.
- **Алгоритм — порт твоего C++:** разностная функция → кумулятивная нормализация → абсолютный порог **0.1** →
  параболическая интерполяция → `f = sampleRate / τ`; нота: `midi = round(69 + 12·log2(f / 440))`.
- **Источники звука (все три):**
  1. **Микрофон** (на собственном сайте getUserMedia работает) — поёшь или играешь, нота встаёт на стан;
  2. **Экранная клавиатура** — синтез тона с гармониками и лёгким шумом, YIN распознаёт его как «чужой» звук;
  3. **Загрузка аудиофайла** — разбор по кадрам, последовательность нот на стане.
- **UI:** нотный стан (скрипичный ключ, SVG), под ним частота в Hz, название ноты, отклонение в центах.
- **Игра «Ear test»:** звучит случайная нота C4–C6, нужно кликнуть по нужной линейке/промежутку стана; серия
  правильных ответов, лучшая серия в `localStorage`.
- **Растяжка (опционально):** скомпилировать *тот же* C++ `PitchDetector` в WebAssembly через Emscripten и подписать
  «this is the same C++ code as the desktop app». Сильный инженерный ход; делать после всего остального.
- **Приёмка:** синтезированные A4 = 440 Hz и C5 распознаются с ошибкой < 5 центов; при отказе в доступе к микрофону
  понятное сообщение и остальные источники работают; звук стартует только по клику.

### 7.10 RailSnake — мини-игра (Depot)

- **Идея:** «змейка» на сортировочной станции: локомотив собирает вагоны на станциях, состав растёт, скорость
  повышается; упор — буферы и собственный хвост.
- **Управление:** стрелки/WASD, свайпы, экранный крестовина на телефоне; пробел — пауза.
- **Графика:** Canvas, пиксельно-чёткая сетка, цвета линий из палитры; вагоны разных цветов = линии сайта.
- **Счёт:** текущий и лучший (`localStorage`). Общего лидерборда **нет** сознательно: публичная таблица с
  анонимными никами на сайте-резюме — риск, что рекрутер увидит мусор.
- **Приёмка:** 60 fps на среднем телефоне; пауза при уходе со вкладки и при скролле за пределы; старт только по
  действию игрока; игра не перехватывает стрелки, пока не в фокусе.

### 7.11 Terminal — пасхалка

- **Открытие:** клавиша `` ` `` или кнопка `›_` в навигации; `Esc` закрывает.
- **Команды:** `help`, `whoami`, `ls` (секции), `cat <section>`, `open <project>` (скролл к проекту),
  `cv` (скачать PDF), `contact`, `theme light|dark`, `play` (к игре), `clear`, `exit`;
  пасхалка `sudo hire makar`.
- **Удобства:** история `↑/↓`, автодополнение `Tab`.
- **Приёмка:** весь вывод берётся из `resume.ts`, не дублирует текст руками; ловушка фокуса внутри окна.

---

## 8. Этапы и промпты для Claude Code

Работай этапами: одна сессия — один этап, в начале этапа проси план (plan mode), после — `npm run build`,
проверка в браузере, коммит, `/clear`. Сайт должен быть рабочим и задеплоенным уже после этапа 1.

**Этап 0 — каркас.**
> Прочитай SPEC.md §2–4. Создай проект Astro (актуальная версия) + TypeScript strict + Svelte 5, подключи шрифты из §4 через
> @fontsource, сделай `tokens.css` с палитрой обеих тем, переключатель темы, `Base.astro`, workflow деплоя на GitHub
> Pages. Пока без контента. Папка не пустая: `SPEC.md`, `CLAUDE.md`, `.gitignore`, `src/data/demo/`, `reference/`,
> `public/Makar_Glazachev_CV.pdf` уже на месте — не перезаписывай и не удаляй их, `.gitignore` только дополняй.
> Покажи план перед тем, как писать код.

**Этап 1 — весь контент статично.**
> Прочитай SPEC.md §5 и §6. Создай `src/data/resume.ts` строго по §6 и свёрстай все секции статично, без
> островов: hero (без карты), experience, hackathons, projects, education, skills, contact, футер, липкая навигация
> без анимации. Кнопка Download CV на `/Makar_Glazachev_CV.pdf`. Адаптив до 360px, доступность по §3.
> Проверь Lighthouse.

**Этап 2 — hero-карта и табло.**
> Реализуй §7.1 и §7.2 и поезд прогресса в навигации из §5. Соблюдай общие правила островов из §2.

**Этапы 3a–3g — по одной демке за сессию,** в порядке ценности для резюме:
3a §7.4 RailMap → 3b §7.9 PitchStaff → 3c §7.8 RouteFinder → 3d §7.3 LocoLookup → 3e §7.5 AgentPipeline →
3f §7.6 TwoModels → 3g §7.7 ApiExplorer.
Первыми идут демки на настоящих артефактах (карта сети, твой C++-алгоритм, твой граф реакций), игрушечные — после.
RailMap — два захода: сначала скрипт выгрузки станций и проверка определения дорог по эталонам, потом сама карта.
> Реализуй SPEC.md §7.X как остров в `src/islands/…`. Соблюдай общие правила из §2 и правила данных из §6.1.
> Напиши юнит-тесты на логику (vitest). Покажи план перед кодом.

**Этап 4 — игра и терминал.** §7.10, §7.11.

**Этап 5 — полировка.** OG-картинка, favicon, 404, мета-теги, финальный прогон Lighthouse по бюджету §3,
проверка на реальном телефоне, опционально WASM-версия YIN и русская версия сайта.

---

## 9. Деплой

- Репозиторий `MakarON-228.github.io` → сайт открывается по адресу `https://makaron-228.github.io` без
  подпути. Если репозиторий назван иначе — задать `base` в `astro.config.mjs`.
- Workflow: официальный `withastro/action` на push в `main`.
- После деплоя: ссылку на сайт добавить в PDF-резюме (строка контактов) и в профиль GitHub.

---

## 10. Открытые вопросы

Решено: даты SIBUR (Jul 2025) и Score Editor (Mar 2026); агенты Яндекса (§7.5, из кода); способ объединения
моделей (§7.6); граф реакций (§7.8); карта — открытые данные OSM, без данных ТМХ (§7.4).

| # | Вопрос | Нужно к этапу |
|---|---|---|
| 1 | Адрес: `makaron-228.github.io` или свой домен | 0 |
| 2 | Источник схемы реакций (книга/диссертация) — для подписи под графом, если хочешь указать | 3c |
| 3 | Русская версия сейчас или позже | 5 |
